/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com','upload.wikimedia.org', 'images.punkapi.com','thumbs.dreamstime.com' ]
      },
}

module.exports = nextConfig
